# easy
- append, extend

# medium
- to/from CSV
- null/missing support in basic functions - should be handled in vec, groupby (indexes), etc.
- apply
- better constructor for DF

# hard
- groupby count, sum, min, max, mean

# won't do

- joins
- groupby transform
